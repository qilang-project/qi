// 字符串函数示例
函数 主() {
    打印("=== 字符串函数测试 ===");

    变量 str1 = "你好";
    变量 str2 = "世界";

    // 字符串连接
    变量 连接结果 = 字符串连接(str1, str2);
    打印("连接结果: " + 连接结果);

    // 字符串长度
    变量 长度结果 = 字符串长度("Qi编程语言");
    打印("字符串长度: " + 长度结果);

    打印("=== 字符串函数测试完成 ===");
}