// 文件操作示例
函数 主() {
    打印("=== 文件操作测试 ===");

    变量 文件路径 = "测试.txt";
    变量 文件内容 = "Hello from Qi Runtime!";

    // 写入文件
    打印("正在写入文件: " + 文件路径);
    写入文件(文件路径, 文件内容);

    // 读取文件
    打印("正在读取文件: " + 文件路径);
    变量 读取内容 = 读取文件(文件路径);
    打印("文件内容: " + 读取内容);

    打印("=== 文件操作测试完成 ===");
}