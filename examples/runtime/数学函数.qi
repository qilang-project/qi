// 数学函数示例
函数 主() {
    打印("=== 数学函数测试 ===");

    变量 a = 16;
    变量 b = -42;
    变量 c = 3.14159;

    // 平方根
    变量 平方根结果 = 求平方根(a);
    打印("16的平方根: " + 平方根结果);

    // 绝对值
    变量 绝对值结果 = 求绝对值(b);
    打印("-42的绝对值: " + 绝对值结果);

    打印("=== 数学函数测试完成 ===");
}