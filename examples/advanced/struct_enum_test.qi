// Test struct and enum functionality

struct Point {
    x: 整数,
    y: 整数
}

enum Color {
    红色,
    绿色,
    蓝色
}

// Create a point instance
let p = Point { x: 10, y: 20 };

// Access struct fields
let x_val = p.x;
let y_val = p.y;

// Use enum values
let c = Color::红色;