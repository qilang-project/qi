// 这是单行注释 (line comment)

/* 这是多行注释 (block comment)
   可以跨越多行 */

/// 这是文档注释 (doc line comment)
/// 通常用于函数、结构体的文档

/**
 * 这是文档块注释 (doc block comment)
 * 可以包含多行文档
 * 支持更详细的描述
 */

结构体 点 {
    /// X 坐标 - 水平位置
    x: 整数;
    /// Y 坐标 - 垂直位置
    y: 整数
}

/// 计算两点之间的距离
/// 参数: p1, p2 - 两个点
/// 返回: 距离的平方值
函数 计算距离(p1: 点, p2: 点) -> 整数 {
    变量 dx = p2.x - p1.x; // 计算 X 轴差值
    变量 dy = p2.y - p1.y; /* 计算 Y 轴差值 */
    返回 dx * dx + dy * dy;
}

变量 p1 = 点 { x: 1, y: 2 }; // 创建第一个点
变量 p2 = 点 { x: 4, y: 6 }; /* 创建第二个点 */
变量 result = 计算距离(p1, p2); // 计算距离