// Qi 编程语言综合演示
// 演示所有主要功能

// 结构体定义
结构体 点 {
    x: 整数;
    y: 整数
}

// 函数定义
函数 计算距离(p1: 点, p2: 点) -> 整数 {
    变量 dx = p2.x - p1.x;
    变量 dy = p2.y - p1.y;
    返回 dx * dx + dy * dy;
}

函数 主程序() -> 整数 {
    // 创建点实例
    变量 p1 = 点 { x: 1, y: 2 };
    变量 p2 = 点 { x: 4, y: 6 };

    // 计算距离
    变量 距离 = 计算距离(p1, p2);

    // 条件判断
    如果 距离 > 25 {
        变量 message = "距离较远";
    } 否则 {
        变量 message = "距离较近";
    }

    返回 距离;
}

// 调用主程序
变量 最终结果 = 主程序();