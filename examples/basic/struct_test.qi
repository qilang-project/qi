// 简单结构体字面量测试

// 定义点结构体
结构体 Point {
    x: 整数,
    y: 整数
}

// 定义带字符串的结构体
结构体 Person {
    name: 字符串,
    age: 整数
}

// 测试空结构体
结构体 Empty {}

函数 main() {
    // 测试简单的结构体字面量
    变量 p = (Point { x: 10, y: 20 });
    打印 p.x;  // 应该输出: 10
    打印 p.y;  // 应该输出: 20

    // 测试嵌套的结构体字面量
    变量 person = (Person {
        name: "张三",
        age: 25
    });
    打印 person.name;  // 应该输出: 张三
    打印 person.age;   // 应该输出: 25

    // 测试空结构体实例
    变量 empty: Empty = (Empty {});
    // 这里我们只是创建了一个空结构体的实例
}