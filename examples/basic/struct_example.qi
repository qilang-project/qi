// 结构体声明示例

// 定义一个简单的 Point 结构体
结构体 Point {
    x: 整数,
    y: 整数
}

// 定义一个 Person 结构体
结构体 Person {
    name: 字符串,
    age: 整数,
    city: 字符串
}

// 定义一个更复杂的 Rectangle 结构体
结构体 Rectangle {
    width: 浮点数,
    height: 浮点数,
    color: 字符串
}

// 定义一个中文字段名的结构体
结构体 学生 {
    姓名: 字符串,
    年龄: 整数,
    成绩: 浮点数
}
