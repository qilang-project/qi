// 绿码：跨文件导入用户模块 + 互调（函数/结构体跨文件可见）—— 零报错
包 主程序;

导入 ./工具库;

// 本文件函数调用导入模块的函数
函数 报距离(x: 整数, y: 整数) : 整数 {
    变量 p = 造坐标(x, y);
    返回 曼哈顿(p);
}

函数 入口() {
    打印行(工具问候("主程序"));

    // 跨文件结构体 + 函数
    变量 p = 造坐标(0 - 3, 4);
    打印行(曼哈顿(p));
    打印行(p.横 + p.纵);

    打印行(报距离(6, 0 - 6));
}
