// 绿码：句柄存进 整数 变量、再当参数传回去 —— 标准库到处都是这个写法，
// 一个字都不许报。注册表里这些返回标的是 "句柄"，检查器对它保持沉默。
包 主程序;

导入 标准库.列表 作为 L;
导入 标准库.哈希表 作为 H;

函数 入口() {
    变量 表: 整数 = L.创建整数列表();
    L.添加整数(表, 7);
    打印行(L.获取整数(表, 0));

    变量 映射: 整数 = H.创建整数表();
    H.设置整数(映射, "甲", 1);
    打印行(H.获取整数(映射, "甲"));
}
