// 绿码：句柄存进 整数 变量、再当参数传回去 —— 标准库到处都是这个写法，
// 一个字都不许报。
//
// 这份语料是「整数返回全部受检」这个决定的安全网：检查器现在会查
// 每一条 FFI 的整数返回，而句柄惯用法必须照旧通过（整数对整数不冲突）。
包 主程序;

导入 标准库.列表 作为 L;
导入 标准库.哈希表 作为 H;

函数 入口() {
    变量 表: 整数 = L.创建整数列表();
    L.添加整数(表, 7);
    打印行(L.获取整数(表, 0));
    打印行(L.整数列表大小(表));

    变量 映射: 整数 = H.创建整数表();
    H.设置整数(映射, "甲", 1);
    打印行(H.获取整数(映射, "甲"));
    打印行(H.表大小(映射));
}
