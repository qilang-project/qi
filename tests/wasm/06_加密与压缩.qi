// 加密（已知向量）与压缩往返 —— 都是纯字节计算，最能暴露 32 位下的越界与截断
包 主程序;

导入 标准库.输入输出 作为 IO;
导入 标准库.加密 作为 密;
导入 标准库.压缩 作为 压;
导入 标准库.字符串;

函数 入口() {
    // 已知向量：SHA256("abc") 的前 16 个十六进制字符
    IO.打印行("SHA256 " + 字符串::子串(密.SHA256哈希("abc"), 0, 16));
    IO.打印行("MD5    " + 字符串::子串(密.MD5哈希("abc"), 0, 16));
    IO.打印行("Base64 " + 密.Base64编码("奇语 qi"));

    变量 原文: 字符串 = "重复重复重复重复重复重复重复重复重复重复";
    变量 回来: 字符串 = 压.解压字符串(压.压缩字符串(原文));
    IO.打印行("压缩往返一致 " + 字符串::等于(原文, 回来));
}
