// bug④回归：`x 作为 T` 表达式 —— 修前 codegen 无此臂，报「变量 y 初值无值」。
// 覆盖：整数↔浮点数、整数/浮点数→字符串、字符串→整数/浮点数、
//       布尔→整数/字符串、整数→布尔、无注解推断。
包 主程序;
导入 标准库.输入输出 作为 IO;
函数 入口() {
    变量 x: 整数 = 42;
    变量 y: 浮点数 = x 作为 浮点数;
    IO.打印行(浮点数转字符串(y));
    变量 f: 浮点数 = 3.99;
    IO.打印行(整数转字符串(f 作为 整数));
    IO.打印行(x 作为 字符串);
    IO.打印行(浮点数转字符串((f 作为 字符串) 作为 浮点数));
    变量 s: 字符串 = "123";
    IO.打印行(整数转字符串((s 作为 整数) + 1));
    变量 b: 布尔 = 真;
    IO.打印行(整数转字符串(b 作为 整数));
    IO.打印行(b 作为 字符串);
    IO.打印行(整数转字符串((0 作为 布尔) 作为 整数));
    变量 z = x 作为 浮点数;
    IO.打印行(浮点数转字符串(z + 0.5));
}
