// bug①回归：嵌套块同名变量遮蔽 —— 内层 变量 x 不得覆盖外层槽。
// 期望输出：3 / 2 / 1（修前输出 3 / 3 / 3）。
包 主程序;
导入 标准库.输入输出 作为 IO;
函数 入口() {
    变量 x: 整数 = 1;
    如果 (1 == 1) {
        变量 x: 整数 = 2;
        如果 (2 == 2) {
            变量 x: 整数 = 3;
            IO.打印行(整数转字符串(x));
        }
        IO.打印行(整数转字符串(x));
    }
    IO.打印行(整数转字符串(x));
}
