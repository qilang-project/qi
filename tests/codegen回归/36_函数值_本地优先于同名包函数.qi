// 非入口模块里把**本模块自己的**顶层函数当函数值传出去时，必须传本模块那个，
// 不能被同名的导入包函数顶掉。
//
// 踩过的真事（家庭待办 项目）：页面.qi 里 `提交(应用值, "/api/del", 删一条)`，
// 而 qi-web 的 资源后台.qi 有个**四个参数**的 公开 函数 删一条。
// 编译器报的是 `trampoline 目标缺失: _Z_E588A0E4B880E69DA12334`
// —— 末尾 #4 是它以为的元数，跟「名字撞车」八竿子打不着，极难往这儿想。
//
// 根因：函数值签名索引表按**裸名**建键、先登记的赢，而导入包先于本地模块登记。
// 入口模块碰不到（它先于包登记），所以只有拆了模块的多文件项目才会中招 ——
// 也就是说，越是像样的工程越容易撞。
//
// 期望 30 = 本地的 删一条(3) = 3 * 10。若解析到包里那个四参的，编译就直接失败。
导入 ./函数值同名/页 作为 页;

函数 入口() {
    打印行(整数转字符串(页.跑()));
}
