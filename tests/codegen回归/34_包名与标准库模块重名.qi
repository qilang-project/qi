// 用户包跟标准库模块重名时，`别名.函数()` 必须解析到**标准库**那个。
//
// 踩过的真事：qi-grpc 是 `包 gRPC;` 外加 `导入 标准库.gRPC 作为 运行时`，
// 两边都有 调用带元数据 / 开流。有一版守卫只按「函数按包 里有没有同名函数」
// 就把标准库调用让给了用户包，于是 `运行时.调用带元数据(五个参数)` 解析到
// 本包那个两参的同名函数上 —— 报的是 LLVM「Incorrect number of arguments」，
// 跟名字撞车八竿子打不着，极难往这儿想。
//
// 这里用 时间 复现同一形状：标准库的 时间 已经是 qi 实现的，用户包也叫 时间、
// 也有 年，但签名不同（两个参数）。
导入 标准库.时间 作为 时钟;
导入 ./包与标准库重名/时间 作为 本地时间;

函数 入口() {
    // 标准库的 年(时间戳) —— 一个参数
    打印行(整数转字符串(时钟.年(1787754645)));
    // 本包的 年(甲, 乙) —— 两个参数
    打印行(整数转字符串(本地时间.年(3, 4)));
}
