// 08/09 号用例的夹具：故意和 界面.转义HTML 同名，制造跨包同名的局面。
//
// 这两条用例原先导入的是**真的 Web 包**（qi-web），本地能过是因为 monorepo
// 里 qi-web/ 就在 qi/ 隔壁、导入解析会扫祖先目录；可 CI 只 clone 了
// qi-gui 和 qi-runtime，于是「无法找到导入模块: Web」——
// 08 直接失败，09 更阴：它是红码用例，确实报了错、但报的是「找不到 Web」
// 而不是「歧义」，断言 grep 不到关键字照样判失败。
//
// 回归用例不该依赖另一个仓当前的内容（哪天 qi-web 把 转义HTML 改个名，
// 这两条也会跟着莫名变红）。11/12 号一开始就自带夹具，这里补齐。
包 网页;

公开 函数 转义HTML(文本: 字符串) : 字符串 {
    返回 "网页:" + 文本;
}
