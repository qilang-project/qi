// 红码：`标准库.日期时间` 根本不存在（真名是 时间 / 日期）。
//
// 以前这行编译通过、跑起来也正常 —— 因为别名恰好叫「时间」，`时间.睡眠毫秒()`
// 会经「别名当模块名」的回退路径解析到真正的「时间」模块。换个别名才会在
// codegen 阶段报「模块没有这个函数」，而错误信息指着一个压根不存在的模块名，
// 让人以为是函数少了。现在在导入这一行就拦住。
包 主程序;

导入 标准库.日期时间 作为 时间;

函数 入口() {
    时间.睡眠毫秒(1);
    打印行("不该走到这");
}
