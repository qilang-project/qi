// 回归：同一个包铺在多个文件时，各文件的**私有**助手可以重名。
//
// 修之前：包内符号名 只含 包+名+元数，两个文件的 `函数 一参(x)` 撞在同一个
// LLVM 符号上，编译报「无法按元数区分」—— 而它们是各自的局部助手，
// 谁也没打算给别人用，被逼着改名（一参 / 作业键参 / 单参…）。
//
// 修之后：第二个及以后占用同一 (包,名,元数) 的**私有**函数带文件前缀，
// 调用点先查本文件那份。头一个保持原符号，所以跨文件调用别人的私有函数照旧。
//
// 仍然必须报错的两种（另见 19 / 20）：同文件重名、任一方是 公开。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 私有助手甲::{甲说};
导入 私有助手乙::{乙说};

函数 入口() {
    IO.打印行(甲说());
    IO.打印行(乙说());
}
