// 红码：同样两个包同名，但**不写**限定名 —— 歧义检查必须照旧生效，
// 不能因为修了 08 就把这条防线一起修没了。
//
// 用本地夹具 网页.qi 而不是真的 Web 包：CI 不 clone qi-web，那时这条会
// **因为错的理由失败** —— 确实报了错，但报的是「找不到 Web」，
// 断言 grep 不到「歧义」照样判红，排查时很误导。
包 主程序;
导入 界面;
导入 网页;
函数 入口() {
    打印(转义HTML("<b>"));
}
