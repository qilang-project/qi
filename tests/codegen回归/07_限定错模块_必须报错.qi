// bug②回归（红码）：JSON 模块没有「解析」——必须报确定的编译错误，
// 绝不允许按名落到 qi_datetime_parse / qi_multipart_parse（修前随机行为）。
// 断言脚本预期：编译失败 + 错误信息含「没有函数」。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 标准库.JSON 作为 JSON;
函数 入口() {
    变量 h: 整数 = JSON.解析("{\"a\":1}");
    IO.打印行(整数转字符串(h));
}
