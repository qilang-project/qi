// 无返回值函数塞进「返回 int」的回调槽：C 会去读一个没人写过的返回寄存器，
// qsort 据此排序 → 结果随机。必须编译期挡住。
外部 "c" {
    函数 qsort(基址: 指针, 个数: 整数, 宽度: 整数, 比较: 函数(指针, 指针): 整数): 空;
}

函数 无返回(甲: 指针, 乙: 指针) { 打印行("x"); }

函数 入口() {
    qsort(0, 0, 8, 无返回);
}
