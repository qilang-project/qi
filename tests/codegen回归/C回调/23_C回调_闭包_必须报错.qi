// 闭包不能当 C 回调：闭包值是 env 对象地址，不是代码指针，C 一 call 就 SIGBUS。
// 宁可编译期缺功能，也不能放过去。
外部 "c" {
    函数 qsort(基址: 指针, 个数: 整数, 宽度: 整数, 比较: 函数(指针, 指针): 整数): 空;
}

函数 入口() {
    变量 门槛: 整数 = 5;
    变量 比较器 = 闭包(甲: 指针, 乙: 指针): 整数 { 返回 门槛; };
    qsort(0, 0, 8, 比较器);
}
