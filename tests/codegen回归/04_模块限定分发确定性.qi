// bug②回归：模块限定重名函数分发必须确定且正确。
// 「解析」注册在 命令行/多部分/时间 三个模块 —— 修前 JSON.解析 会按
// HashMap 迭代序随机分发到 qi_datetime_parse / qi_multipart_parse。
// 本例用正确的 时间.解析 —— 断言脚本额外做「编译 5 次输出一致」校验。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 标准库.时间 作为 时间;
函数 入口() {
    变量 h: 整数 = 时间.解析("2026-07-17 08:30:00", "%Y-%m-%d %H:%M:%S");
    IO.打印行(整数转字符串(时间.年(h)));
    IO.打印行(整数转字符串(时间.月(h)));
}
