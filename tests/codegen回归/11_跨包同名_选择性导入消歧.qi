// 回归：选择性导入必须能消歧「导入路径 ≠ 声明包名」的跨包同名函数。
//
// 甲/乙.qi 声明的是 `包 乙;`（不是 包 甲 也不是 包 甲.乙）—— 真实项目里
// qi-web/账户.qi 就这么写（路径 Web.账户、声明 `包 账户;`）。修前导入来源只
// 按全路径（重载集另试首段）对包名，末段这档缺失：`导入 甲.乙::{同名报数}`
// 写了也白写，照样报「函数名 同名报数 歧义」，而报错建议的正是这个写法。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 丙;
导入 甲.乙::{同名报数};
函数 入口() {
    IO.打印行(同名报数("x"));
}
