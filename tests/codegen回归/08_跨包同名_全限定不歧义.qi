// 回归：两个包各有一个 转义HTML，调用点写全限定名时必须各自解析到对的包。
//
// 修前 module_qualifier 只被记录、解析时完全不看，限定名被剥掉退回按名查找，
// 于是报「函数名 转义HTML 歧义：定义于多个包（Web、界面）」—— 可调用点明明
// 已经写全了，用户只能给自己的函数改名绕开。
//
// 第二个包原先用的是真的 Web（qi-web），现在换成本地夹具 网页.qi ——
// CI 不 clone qi-web，理由见 网页.qi。
包 主程序;
导入 标准库.输入输出 作为 IO;
导入 界面;
导入 网页;
函数 入口() {
    IO.打印行(界面.转义HTML("<b>"));
    IO.打印行(网页.转义HTML("<b>"));
}
