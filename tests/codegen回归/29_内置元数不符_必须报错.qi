包 试;

// 回归：撞上无限定并发原语但实参个数不符，必须是**编译错误**，不是编译器 panic。
//
// 生成同步内建 以前直接 `arguments[i]` 索引 —— `等待组增加(组)` 忘了第二个参数，
// 编译器当场：
//   thread 'main' panicked at src/codegen/inkwell_gen/并发.rs:90:30:
//   index out of bounds: the len is 1 but the index is 1
// 报错里连函数名和源码位置都没有。现在让路给后面的解析链，谁都不认时报出
// 「内置并发原语「等待组增加」需要 2 个实参」。

函数 入口() {
    变量 组: 整数 = 创建等待组();
    等待组增加(组);
}
