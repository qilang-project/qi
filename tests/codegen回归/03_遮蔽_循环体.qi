// bug①回归（循环变体）：对于 循环变量遮蔽外层同名变量、循环体内块遮蔽、
// 当 循环体内遮蔽 —— 循环结束后外层值全部完好。
包 主程序;
导入 标准库.输入输出 作为 IO;
函数 入口() {
    变量 i: 整数 = 100;
    变量 和: 整数 = 0;
    对于 i 在 0 直到 3 {
        变量 x: 整数 = i * 10;
        和 = 和 + x;
    }
    IO.打印行(整数转字符串(i));
    IO.打印行(整数转字符串(和));
    当 (和 > 0) {
        变量 和: 整数 = -1;
        IO.打印行(整数转字符串(和));
        跳出;
    }
    IO.打印行(整数转字符串(和));
}
