// 红码：标准库返回**真整数**的函数，结果流进字符串槽 —— 必须报 TypeMismatch。
//
// 注册表原先用同一个「整数」既表示真整数（时间.年() 的年份、字符串::查找()
// 的下标）又表示不透明句柄（数据库.连接() 的连接号），检查器分不出来，
// 只能按最保守的那种处理 —— **307 条整数返回全部沉默**，这一行一路放到 codegen。
// 现在句柄在注册表里显式写成 "句柄"，剩下的整数就是真整数。
包 主程序;

导入 标准库.时间 作为 时间;

函数 入口() {
    变量 t: 整数 = 时间.现在();
    变量 岁: 字符串 = 时间.年(t);
    打印行(岁);
}
