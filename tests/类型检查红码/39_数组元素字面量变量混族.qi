// 红码：数组元素混族，但错的那一个是**字面量变量**而非直接字面量。
//
// 33 号只覆盖 `[1, 2, "三"]`（全是直接字面量）。用一个变量中转就能绕过去：
// 实测 `变量 表: 数组<整数> = [甲, 乙]` 编译通过、退出码 0，打印出来是
// 字符串的堆地址（4359364632 这种）—— 静默错答案里最难发现的一类。
包 主程序;

函数 入口() {
    变量 甲: 整数 = 1;
    变量 乙: 字符串 = "不是整数";
    变量 表 = [甲, 乙];
    打印行(1);
}
