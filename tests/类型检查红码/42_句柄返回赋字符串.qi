// 红码：不透明句柄（数据库连接号）流进字符串槽 —— 必须报。
//
// 这一条曾经是**故意放过**的：注册表把「整数」既当真整数又当句柄，
// 检查器分不清，只好一律沉默。全语料实验（368 条句柄全部改成受检，
// 567 文件 0 报错）证明那条顾虑不成立 —— 句柄惯用法是「句柄存进 整数
// 变量」，整数对整数不冲突，跟这里的 字符串 槽没关系。
包 主程序;

导入 标准库.数据库 作为 数据库;

函数 入口() {
    变量 连接: 字符串 = 数据库.连接("试.db");
    打印行(连接);
}
