// 红码：模块调用的**字符串返回值**赋给 整数 变量 —— 必须报 TypeMismatch。
//
// 真实翻车现场：数据库.执行参数 返回的是 JSON 字符串（注册表标 "ptr"，
// 实现是 *mut c_char），第一版闪购把它当受影响行数赋给整数，编译器沉默、
// 运行时指针永远非零 —— 库存不够的单也全报成交，20 路并发抢购才暴露。
// 两个条件缺一不可：① 注册表返回 "ptr" 映射成字符串；② 模块限定调用
// 且返回类型具体时算可信来源（不然「初值不是字面量」直接跳过比对）。
包 主程序;

导入 标准库.数据库;

函数 入口() {
    变量 库: 整数 = 数据库.连接(":memory:");
    变量 动了: 整数 = 数据库.执行参数(库, "UPDATE t SET n = 1", "[]");
    打印行(整数转字符串(动了));
}
