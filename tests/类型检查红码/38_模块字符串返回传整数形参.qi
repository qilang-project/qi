// 红码：模块调用的字符串返回值直接传给**整数形参** —— 必须报 TypeMismatch。
// 同 37 的另一条路径：不经变量中转，实参位置直接错。
// Redis.取 返回字符串（注册表标 "ptr"），时间.睡眠毫秒 要整数。
包 主程序;

导入 标准库.Redis 作为 Redis;
导入 标准库.时间 作为 时间;

函数 入口() {
    变量 连: 整数 = Redis.连接("redis://127.0.0.1:1/0");
    时间.睡眠毫秒(Redis.取(连, "键"));
}
