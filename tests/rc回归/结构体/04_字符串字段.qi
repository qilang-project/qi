// 场景 04：结构体字段是字符串 —— 本体归零时字段串必须一并释放。
// 用拼接产生真堆串（字面量是 immortal，测不出 RC 路径）。
包 主程序;

类型 名片 {
    姓名: 字符串,
    工号: 整数,
}

函数 造名片(序号: 整数): 整数 {
    变量 卡: 名片 = 新建 名片 { 姓名: "员工" + 整数转字符串(序号), 工号: 序号 };
    返回 字符串::字节长度(卡.姓名) + 卡.工号;
}

函数 入口() {
    变量 累计: 整数 = 0;
    变量 i: 整数 = 0;
    当 i < 100 {
        累计 = 累计 + 造名片(i);
        i = i + 1;
    }
    打印行(累计);
}
