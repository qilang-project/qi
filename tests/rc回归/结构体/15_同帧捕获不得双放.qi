// 场景 15：抛出与捕获在**同一个函数**里 —— 抛出前不许释放本帧 RC 局部
// （longjmp 回到本函数的 catch，后面还要读它们，函数出口还会再放一次）。
// 这条是「抛出前释放局部」那个修复的安全护栏：崩了/输出错了就是双放。
包 主程序;

类型 点 {
    横: 整数,
    纵: 整数,
}

函数 同帧(序号: 整数): 整数 {
    变量 p: 点 = 新建 点 { 横: 序号, 纵: 序号 };
    变量 名: 字符串 = "点" + 整数转字符串(序号);
    变量 累计: 整数 = 0;
    尝试 {
        如果 序号 % 2 == 0 {
            抛出 "偶数不收";
        }
        累计 = 累计 + p.横;
    } 捕获 错误 {
        累计 = 累计 + 1;
    }
    // longjmp 之后这两个局部必须还活着
    返回 累计 + p.纵 + 字符串::字节长度(名);
}

函数 入口() {
    变量 累计: 整数 = 0;
    变量 i: 整数 = 0;
    当 i < 100 {
        累计 = 累计 + 同帧(i);
        i = i + 1;
    }
    打印行(累计);
}
