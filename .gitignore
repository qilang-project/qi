# Rust build artifacts
/target/
/debug/
/release/
*.rs.bk
*.rlib
*.prof*

# Cargo
Cargo.lock

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Environment files
.env*
!.env.example

# Qi compiler specific
*.qi.d
*.qi.o
*.qi.bc
*.ll
*.s
*.exe
*.o

# Runtime library build artifacts
runtime/build/
runtime/bin/
runtime/lib/
runtime/*.so
runtime/*.dylib
runtime*.dll

# Test coverage
coverage/
tarpaulin-report.html

# Benchmark results
benchmarks/results/
*.benchmark

# Temporary files
*.tmp
*.temp
.cache/

# CMake build files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
# 只忽略 CMake **生成**的 Makefile（在 CMakeFiles/ 同级的构建目录里），
# 仓库根手写的 Makefile 是构建入口，必须提交
build/Makefile

# Documentation build
docs/target/
book/

# Performance profiling
perf.data*
*.profraw
*.profdata

# Clippy
clippy.toml

# Rustfmt
rustfmt.toml

# LLVM intermediate files
*.bc
*.ll
# Playwright MCP runtime artifacts
.playwright-mcp/

# 编译产物：MCP 服务器示例的可执行文件（无后缀）
示例/标准库/MCP服务器/http服务
示例/标准库/MCP服务器/stdio服务
示例/标准库/MCP服务器/通知服务
示例/标准库/MCP服务器/通知服务HTTP

# wasm spike：worktree 里的独立构建目录 + wasm 演示产物
target-wt/
wasm演示/*.wasm
wasm演示/*.o

# 验收脚本的现场工作目录（造静态库/编产物，跑完即删；异常退出会留下）
tests/ffi链接/临时/
tests/绑定生成/构建/
