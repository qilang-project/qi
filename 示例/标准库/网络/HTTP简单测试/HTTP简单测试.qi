包 主程序;

导入 标准库.http;

函数 入口() {
    打印行("=== HTTP 模块简单测试 ===");

    打印行("\n1. GET 请求：");
    变量 响应: 字符串 = http.获取("https://example.com");
    打印("响应: ");
    打印行(响应);

    打印行("\n2. 获取状态码：");
    变量 状态码: 整数 = http.获取状态码("https://example.com");
    打印("状态码: ");
    打印行(状态码);

    打印行("\n=== HTTP 模块测试完成！ ===");
}
