包 工具二;

// 插件 v2 —— 与 v1 同名符号（chuli / wenhou），但行为升级：
//   处理: x*2 → x*3     问候: "v1 你好" → "v2 你好"
// 主进程卸载 v1 后加载它，同一进程内行为即刻切换，不重启。
//
// 构建：qi compile --库 动态 插件v2.qi -o 工具v2.plugin

导出 "chuli" 函数 处理(x: 整数): 整数 {
    返回 x * 3;
}

导出 "wenhou" 函数 问候(名: 字符串): 字符串 {
    返回 "v2 你好, " + 名;
}
