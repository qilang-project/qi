包 工具一;

// 插件 v1 —— 用反向 FFI（导出 函数）编成动态库，供主进程 dlopen 热加载。
// 用显式 ASCII C 符号名（chuli / wenhou），主进程 dlsym 按这些名字取函数。
//
// 构建：qi compile --库 动态 插件v1.qi -o 工具v1.plugin

导出 "chuli" 函数 处理(x: 整数): 整数 {
    返回 x * 2;
}

导出 "wenhou" 函数 问候(名: 字符串): 字符串 {
    返回 "v1 你好, " + 名;
}
