// 小函数与内联示例
包 主程序;

// qi 没有 `内联` 关键字——语法里没有这条规则，写 `内联 函数 …` 会报
// 「意外的标记 `函数`」。展开与否是后端的决定，不是源码里的声明：
// 小而热的函数由编译器自己判断，源码这边只管把它写小、写清楚。
//
// 所以「内联函数」在 qi 里就是普通函数。真正能做的是把该拆的拆出来，
// 让每个函数只做一件事——这也正是值得被展开的那种函数。

函数 平方(x: 整数): 整数 {
    返回 x * x;
}

函数 立方(x: 整数): 整数 {
    返回 x * 平方(x);        // 复用上一个，而不是重写一遍 x * x * x
}

函数 入口() {
    打印("=== 小函数示例 ===");

    变量 n = 5;

    打印("5 的平方:");
    打印(平方(n));

    打印("5 的立方:");
    打印(立方(n));

    // 小函数拼起来仍然是一句话的事
    打印("平方再平方:");
    打印(平方(平方(n)));
}
